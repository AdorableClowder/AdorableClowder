div(ng-controller="signupController as signup")
  .chosen-wants.row(ng-hide="signup.chooseOffers")
    .row
      h2 Choose a few categories that you'd like to learn about!
      div(ng-repeat="want in signup.initialWants" ng-click="signup.toggleWant(want)") {{want}}
    .row
      .col-md-4
        h4 Language Learning
        .checkbox(ng-repeat="subject in signup.sampleCategories.language")
          label
            input(type="checkbox" value="{{subject}}" ng-checked="signup.initialWants.indexOf(subject) > -1"
            ng-click="signup.toggleWant(subject)")
            | {{subject}}
        form(ng-submit="signup.toggleWant(customWant1)")
          input.form-control(ng-model="customWant1" type="text" placeholder="Enter your Own Want" size='30')
          input.btn.btn-primary(type="submit" value="add")
      .col-md-4
        h4 Technology
        .checkbox(ng-repeat="subject in signup.sampleCategories.technology")
          label
            input(type="checkbox" value="{{subject}}" ng-checked="signup.initialWants.indexOf(subject) > -1"
            ng-click="signup.toggleWant(subject)")
            | {{subject}}
        form(ng-submit="signup.toggleWant(customWant2)")
          input.form-control(ng-model="customWant2" type="text" placeholder="Enter your Own Want" size='30')
          input.btn.btn-primary(type="submit" value="add")
      .col-md-4
        h4 Sports
        .checkbox(ng-repeat="subject in signup.sampleCategories.sports")
          label
            input(type="checkbox" value="{{subject}}" ng-checked="signup.initialWants.indexOf(subject) > -1"
            ng-click="signup.toggleWant(subject)")
            | {{subject}}
        form(ng-submit="signup.toggleWant(customWant3)")
          input.form-control(ng-model="customWant3" type="text" placeholder="Enter your Own Want" size='30')
          input.btn.btn-primary(type="submit" value="add")
    .row
      .col-md-4
        h4 Knowledge
        .checkbox(ng-repeat="subject in signup.sampleCategories.knowledge")
          label
            input(type="checkbox" value="{{subject}}" ng-checked="signup.initialWants.indexOf(subject) > -1"
            ng-click="signup.toggleWant(subject)")
            | {{subject}}
        form(ng-submit="signup.toggleWant(customWant4)")
          input.form-control(ng-model="customWant4" type="text" placeholder="Enter your Own Want" size='30')
          input.btn.btn-primary(type="submit" value="add")
      .col-md-4
        h4 Wild 'n Wacky
        .checkbox(ng-repeat="subject in signup.sampleCategories.wild")
          label
            input(type="checkbox" value="{{subject}}" ng-checked="signup.initialWants.indexOf(subject) > -1"
            ng-click="signup.toggleWant(subject)")
            | {{subject}}
        form(ng-submit="signup.toggleWant(customWant5)")
          input.form-control(ng-model="customWant5" type="text" placeholder="Enter your Own Want" size='30')
          input.btn.btn-primary(type="submit" value="add")
      .col-md-4
        h4 Business
        .checkbox(ng-repeat="subject in signup.sampleCategories.business")
          label
            input(type="checkbox" value="{{subject}}" ng-checked="signup.initialWants.indexOf(subject) > -1"
            ng-click="signup.toggleWant(subject)")
            | {{subject}}
        form(ng-submit="signup.toggleWant(customWant6)")
          input.form-control(ng-model="customWant6" type="text" placeholder="Enter your Own Want" size='30')
          input.btn.btn-primary(type="submit" value="add")
    .row
      .col-md-4
        h4 Craft and Design
        .checkbox(ng-repeat="subject in signup.sampleCategories.craftAndDesign")
          label
            input(type="checkbox" value="{{subject}}" ng-checked="signup.initialWants.indexOf(subject) > -1"
            ng-click="signup.toggleWant(subject)")
            | {{subject}}
        form(ng-submit="signup.toggleWant(customWant7)")
          input.form-control(ng-model="customWant7" type="text" placeholder="Enter your Own Want" size='30')
          input.btn.btn-primary(type="submit" value="add")
    .row
      .btn.btn-lg.btn-primary(ng-click="signup.chooseOffers = true") Select These Wants

  .chosen-offer.row(ng-show="signup.chooseOffers")
    .row
      .btn.btn-sm.btn-default(ng-click="signup.chooseOffers = false") Go Back
      h2 And how about a few things you'd like to 
      div(ng-repeat="offer in signup.initialOffers") {{offer}}
    .row
      .col-md-4
        h4 Language Learning
        .checkbox(ng-repeat="subject in signup.sampleCategories.language")
          label
            input(type="checkbox" value="{{subject}}" ng-checked="signup.initialWants.indexOf(subject) > -1"
            ng-click="signup.toggleWant(subject)")
            | {{subject}}
        form(ng-submit="signup.toggleWant(customWant1)")
          input.form-control(ng-model="customWant1" type="text" placeholder="Enter your Own Want" size='30')
          input.btn.btn-primary(type="submit" value="add")
      .col-md-4
        h4 Technology
        .checkbox(ng-repeat="subject in signup.sampleCategories.technology")
          label
            input(type="checkbox" value="{{subject}}" ng-checked="signup.initialWants.indexOf(subject) > -1"
            ng-click="signup.toggleWant(subject)")
            | {{subject}}
        form(ng-submit="signup.toggleWant(customWant2)")
          input.form-control(ng-model="customWant2" type="text" placeholder="Enter your Own Want" size='30')
          input.btn.btn-primary(type="submit" value="add")
      .col-md-4
        h4 Sports
        .checkbox(ng-repeat="subject in signup.sampleCategories.sports")
          label
            input(type="checkbox" value="{{subject}}" ng-checked="signup.initialWants.indexOf(subject) > -1"
            ng-click="signup.toggleWant(subject)")
            | {{subject}}
        form(ng-submit="signup.toggleWant(customWant3)")
          input.form-control(ng-model="customWant3" type="text" placeholder="Enter your Own Want" size='30')
          input.btn.btn-primary(type="submit" value="add")
    .row
      .col-md-4
        h4 Knowledge
        .checkbox(ng-repeat="subject in signup.sampleCategories.knowledge")
          label
            input(type="checkbox" value="{{subject}}" ng-checked="signup.initialWants.indexOf(subject) > -1"
            ng-click="signup.toggleWant(subject)")
            | {{subject}}
        form(ng-submit="signup.toggleWant(customWant4)")
          input.form-control(ng-model="customWant4" type="text" placeholder="Enter your Own Want" size='30')
          input.btn.btn-primary(type="submit" value="add")
      .col-md-4
        h4 Wild 'n Wacky
        .checkbox(ng-repeat="subject in signup.sampleCategories.wild")
          label
            input(type="checkbox" value="{{subject}}" ng-checked="signup.initialWants.indexOf(subject) > -1"
            ng-click="signup.toggleWant(subject)")
            | {{subject}}
        form(ng-submit="signup.toggleWant(customWant5)")
          input.form-control(ng-model="customWant5" type="text" placeholder="Enter your Own Want" size='30')
          input.btn.btn-primary(type="submit" value="add")
      .col-md-4
        h4 Business
        .checkbox(ng-repeat="subject in signup.sampleCategories.business")
          label
            input(type="checkbox" value="{{subject}}" ng-checked="signup.initialWants.indexOf(subject) > -1"
            ng-click="signup.toggleWant(subject)")
            | {{subject}}
        form(ng-submit="signup.toggleWant(customWant6)")
          input.form-control(ng-model="customWant6" type="text" placeholder="Enter your Own Want" size='30')
          input.btn.btn-primary(type="submit" value="add")
    .row
      .col-md-4
        h4 Craft and Design
        .checkbox(ng-repeat="subject in signup.sampleCategories.craftAndDesign")
          label
            input(type="checkbox" value="{{subject}}" ng-checked="signup.initialWants.indexOf(subject) > -1"
            ng-click="signup.toggleWant(subject)")
            | {{subject}}
        form(ng-submit="signup.toggleWant(customWant7)")
          input.form-control(ng-model="customWant7" type="text" placeholder="Enter your Own Want" size='30')
          input.btn.btn-primary(type="submit" value="add")
